---
description: "最高优先级：全局核心工作流与架构约束 (Highest Priority)"
globs: "*"
---
# 核心架构与开发工作流约束 (Core Workflow & Architecture Guardrails)

本项目受到极高标准的大厂级文档体系约束。作为 AI 辅助开发者，你必须严格遵循以下规则。任何违背以下原则的代码生成或文档篡改都会被拒绝并要求重写。

## 1. 强制文档阅读顺序 (Required Context Initialization)
在开始任何新任务或新对话前，你必须**按以下顺序**读取项目上下文：
1. **[必须读]** `memory-bank/architecture.md`：建立全局文件结构与职责心智模型，严禁搞错文档的作用。
2. **[必须读]** `memory-bank/implementation-plan.md`：确认当前处于哪个开发阶段（查找未勾选的 `- [ ]` Task），**禁止偏离计划自由发挥**。
3. **[按需读]** 依当前 Task 性质查阅对应的定义文档：
   - 业务/主流程逻辑：查阅 `PRD.md`（唯一需求源）。
   - UI交互/数据结构：查阅 `memory-bank/design-document.md`。
   - 工程实现/第三方库引用：查阅 `memory-bank/tech-stack.md`。
   - 具体的数据、阈值、提示词文案：查阅 `memory-bank/spec-supplement.md`、`game-data.json` 或 `supabase/functions/chat-completion/prompts/system-prompt.txt`。

## 2. 单向依赖与真源红线 (Strict Source of Truth)
- **文档优先级**：`PRD.md` > `design-document.md` > `tech-stack.md` > `implementation-plan.md`。
- **禁止反向改写**：**绝对禁止**因为代码实现遇到困难而反向去修改前序文档的设计。若实现与设计冲突，必须向 User 提问决策。
- **禁止硬编码 (No Hardcoding)**：
  - 严禁在代码中直接写死 AI 提示词（须从 `supabase/functions/chat-completion/prompts/system-prompt.txt` 获取）。
  - 严禁在代码中写死剧情节点数据（须以 `game-data.json` 为准）。
  - 严禁在UI代码写死大段文案，如 Loading、兜底提示（须以 `spec-supplement.md` 为准）。

## 3. 代码分层与工程规范 (Code Modularity & Quality)
- **拒绝单巨石文件**：严禁生成包含数百行代码的单个组件或文件。必须严格遵照 `memory-bank/tech-stack.md` §2 中的 `Feature-based` 目录规范拆分代码（拆分 `components`, `hooks`, `services`, `views` 等）。
- **组件形态强制约束**：
  - 所有按钮必须是胶囊型（如 `rounded-full`）。
  - 所有卡片必须是高圆角（如 `rounded-2xl`）。
  - 所有动效禁止线性动画，必须采用 Spring 弹簧动效（基于 `framer-motion`）。
- **专业规范**：添加清晰的方法和重要组件注释（TSDoc），确保类型定义完整（首选 Zustand + TypeScript），做好异常和边界抓取。

## 4. 状态同步与里程碑更新 (State tracking)
- **步骤反馈**：每完成 `memory-bank/implementation-plan.md` 中的一个 Step（如 Step 1, Step 2），必须主动用 `✔` 勾选它并保存。
- **进度登记**：每完成一个完整的里程碑/阶段，必须在 `memory-bank/progress.md` 中增加记录，包含已完成事项和下一事项。
- **决策存档**：开发过程中遇到需要 User 决定的关键技术方案或产品妥协，完成后需录入 `memory-bank/decision-log.md`。

> **AI 提醒**：这是大型工程的唯一协同法则。少返工的秘诀在于：动手写代码之前，先确保你完全读懂了对应的 Plan 和 Design。不清楚的地方，务必向前端发起提问。