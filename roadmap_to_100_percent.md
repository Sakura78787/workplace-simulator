# 沉浸式岗位试跑模拟器 - 决战完整 PRD 实施路径图 (Roadmap to 100%)

> **背景定义**：本项目当前已完成 MVP 的核心阶段（骨架、主线逻辑、LLM 接入、双层风控、本地续玩与图鉴存储，即 Task 1 - Task 11）。为100%覆盖并落地《PRD.md》设定的所有目标（含 P0 验收与 P1 规划），现根据 `PRD > design > tech-stack > implementation-plan` 的优先级，重新梳理出从当前节点直到产品完全大成的执行计划。

---

## 阶段五：MVP 终体验收与打磨 (Task 12)
*本阶段对应 PRD §7（上线验收标准）及 §6（非功能需求），目标是彻底清扫体验死角，达到真实可传播的可用状态。*

### 12.1 安卓优先回归 (Android & X5 核心验证)
- [ ] **视觉底座回归**：验证 PC 端是否严格锁定 414px 宽居中（防止企业后台风），Android 微信 X5 浏览器下 `100dvh` 是否存在底部输入框被原生导航栏遮挡问题。
- [ ] **交互死角回归**：
  - `StoryCard` 的滑卡偏转阈值（|dx| ≥ 60px）和回弹反馈（必须是 spring，禁线性硬切）。
  - 打字机输出期间（`isTyping=true`），底部快捷芯片和输入框必须被物理锁定，禁止用户插嘴。
  - 输入框聚焦时，上方的快捷选项必须透明渐隐并下移（不挤压卡片高度）。
- [ ] **生成与保存回归**：验证 Android 微信长按 `ResultView` 中的小票海报是否能唤起原生的“保存图片/分享”菜单（考察 `html2canvas` 的跨域和渲染变形问题）。

### 12.2 核心机制防穿透 (数值与状态机测试)
- [ ] **数值边界穿透**：强制输入极端选项，确保单项扣分超过当前值时，数值稳稳夹在 `[0, 100]`，不会出现负数导致 UI 崩溃。跌幅 >20 分的红框闪烁特效必须触发。
- [ ] **复活闭环校验**：确保触发首次死亡时，使用“复活奶茶”能精准回滚到本轮开始前的快照（数值、回合数），且单局只能触发 1 次；“放弃复活”则顺滑进入结算。

### 12.3 降级与风控真机演练 (断网与发疯测试)
- [ ] **3秒与5秒降级法则**：使用 Chrome DevTools 的 Network Throttling 模拟慢网：
  - 第 3 秒必须准时出现趣味文案（“老板正在打字...”）。
  - 第 5 秒接口未返回或直接 500 报错时，必须硬切本地兜底流程（固定不可用提示 + 惩罚扣分），绝对不允许白屏卡死。
- [ ] **隐藏结局与合规**：输入“涉政/辱骂/放弃推进”等危险词汇，验证 Edge Function 的第二层语义审查是否生效（转换为 `block` 或输出安全提示并附加隐藏标签）。

---

## 阶段六：P1 次要与支撑功能 (后 MVP 迭代)
*本阶段对应 PRD §3.1（P1 次要与支撑功能），在本地图鉴与主线跑通后启动首轮运营迭代。*

### Task 13: 分享按钮 A/B 测试体系
- [ ] **文案实验组埋点**：在 `ResultView` 植入 A/B 方案切换逻辑。按设备本地 UUID 固化分配（Sticky，50/50 概率），保证同用户体验一致。
  - Variant A：主打自嘲（如“查看我的病危通知书”）。
  - Variant B：主打挑衅 PK（如“你行你上啊”）。
- [ ] **数据收集**：前端接入轻量级埋点分析。转化率仅看“点击分享/保存按钮”的次数，在 Supabase 建极简表（`ab_clicks`）记录 uuid、variant 与点击事件即可，不搞复杂漏斗。

### Task 14: 图鉴云同步与轻社交榜单 (Cloud & Social)
- [ ] **账户体系（轻量化）**：彻底砍掉微信/手机号等登录机制，纯依赖本地生成的匿名 UUID 进行静默同步。
- [ ] **云端图鉴上报**：将 `historyPokedex` 中的极简元数据同步至 Supabase 数据库 `pokedex_sync` 表，实现跨设备（需携带 UUID）或仅用于后台数据大盘。
- [ ] **全球比惨榜单**：基于结算的“热力百分比”和“存活回合数”，建立只读的“极简匿名打工人热力排行榜”，不做防刷，在 `LandingView` 提供入口。

---

## 阶段七：跨端兼容与全域覆盖 (iOS 与多端适配)
*~~本阶段对应 PRD §6.2 与 6.1 的远期要求，解决 iOS 生态特有兼容问题。~~* **(本期 MVP 明确取消，放弃 iOS 兼容性深调)**

### ~~Task 15: iOS Safari & 微信内核深度适配~~
- [ ] ~~**橡皮筋效果抑制**：处理 iOS Safari 滑动到底部或顶部时触发的原生回弹（橡皮筋），保证 `StoryCard` 拖拽手感纯粹。~~
- [ ] ~~**海报导出攻坚**：修复 iOS 微信中 `html2canvas` 常见的偶发白屏、字体错位（特别是 Smiley Sans 加载问题）以及跨域图片的 CORS 污染问题。~~
- [ ] ~~**虚拟键盘顶起适配**：处理 iOS 中呼出键盘时，`fixed` 或 `absolute` 吸底元素错位悬空的问题。~~

---

## 阶段八：内容矩阵扩充与运营裂变 (Growth & Content)
*MVP 验证模式成功后，扩大产品包容度。*

### Task 16: 新角色与多模态剧本
- [ ] **扩充角色骨架**：在现有的 PM（产品）与 Ops（运营）之外，新增 RD（研发开发）与 QA（测试）。绝对不改底层机制（依然是这 3 项指标 + 5 大固定阶段：热身-甩锅-插单-死线-背锅），仅在 `game-data.json` 扩充文案换皮，节省开发成本。

### Task 17: 深度裂变闭环
- [ ] **海报 Deep Link 引流**：用户扫描别人分享的死因海报二维码时，统一直达项目部署后的网页首页。纯做引流让新用户从头体验即可，不做复杂的场景空降或透传死者状态参数。